export type UserConfig = {
    showsAnswer: boolean,
    addition: boolean,
    substraction: boolean,
    multiplication: boolean,
    division: boolean,
    rewriting: boolean,
    encryption: boolean,

};
