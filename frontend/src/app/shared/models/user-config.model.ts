export type UserConfig = {
    showsAnswer: boolean,
    addition: boolean,
    subtraction: boolean,
    multiplication: boolean,
    division: boolean,
    rewrite: boolean,
    encryption: boolean,
    word: boolean,
};
